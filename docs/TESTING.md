# Testing

Run formatter, analyzer, Flutter tests and database validation as listed in the README. Manual release QA must cover airplane-mode installation, iPhone↔Android invitation and encrypted QR exchange, camera denial, interrupted scans, large text, reduced motion, backup exclusion, forced-close resume, and explicit session deletion.
